# TennixAI Claude Code 启动入口

开始任何分析或修改前：

1. 完整阅读 `AGENTS.md`，并遵守其中的共同协作规则。
2. 按顺序完整阅读 `PROJECT.md`、`ROADMAP.md`、`CURRENT.md`。
3. 核对当前 Git 分支、HEAD、远程和未提交改动后，再领取或继续 `CURRENT.md` 中的唯一主任务。

本文件不保存项目状态，也不复制三份总控内容。项目状态发生变化时，只更新对应总控文件；共同 Agent 流程发生变化时，更新 `AGENTS.md`，本文件继续作为 Claude Code 的稳定入口。
