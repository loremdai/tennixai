# T95–T96 Data Field Integrity Matrix

Status: T95 match-field audit and T96 player/history appendix complete. Every canonical field below is traced through provider mapping, persistence/reducer, REST/SSE, and UI or explicitly marked unavailable/unknown. Confirmed defects have regression evidence.

Evidence: API-Tennis [REST documentation](https://api-tennis.com/documentation), [WebSocket documentation](https://api-tennis.com/documentation_websocket), sanitized fixtures under `backend/tests/fixtures/api_tennis/`, canonical models, repositories/reducer, response DTOs, and page consumers. REST and WebSocket docs show event identity/status, scheduled date/time, players, tournament, score rows, server, PBP and statistics; they do not document tie-break encoding, PBP flag value semantics, source-update timestamps, indoor/format, or the meaning of every omitted WebSocket field. Their examples show empty PBP/score/stat arrays and nullable round.

Shared path: API-Tennis DTO → `api_tennis.py` canonical mapper → `MatchSnapshot`; the realtime worker applies `reduce_live_snapshot`; `MatchSnapshotRepository` persists canonical match/player/tournament, score state, freshness, points, statistics, momentum and quality. REST `/matches/{id}` serializes `MatchSnapshot`; SSE `ready` serializes the same shape and `match_delta` carries reducer changes. Frontend mirrors it in `frontend/lib/api/types.ts`. Statuses: `verified`, `bug fixed`, or `unavailable / undocumented by provider contract`.

## Canonical fields

| Field | Provider source / mapping | Persistence, transport, display; absent value and units | Evidence / status |
|---|---|---|---|
| `DataFreshness.provider` | API-Tennis mapper sets `api_tennis`; legacy P1 LiveTennis sets `livetennis` | Persisted with the snapshot's freshness JSON and returned unchanged; legacy rows without saved freshness return `unknown`, never a false storage-provider identity. Not shown on match hero. | `test_live_maps_vendor_payload_to_canonical`; `test_live_list_maps_vendor_payload_to_canonical`; `test_load_snapshot_rebuilds_the_canonical_view`; bug fixed |
| `DataFreshness.source_updated_at` | API-Tennis REST/WS documents no source-update timestamp; legacy LiveTennis score may supply `timestamp` | API-Tennis: null; LiveTennis: parsed UTC source time; persisted when present; existing rows without it stay null | Official API/WS docs; `test_live_list_maps_vendor_payload_to_canonical` in LiveTennis suite; `test_load_snapshot_rebuilds_the_canonical_view`; unavailable from current P2 provider; persistence loss fixed |
| `DataFreshness.observed_at` | Local receive/mapping clock; not vendor event time | Stored with freshness JSON; snapshot `as_of` is the persisted snapshot clock; UI formats it in Beijing time (`Asia/Shanghai`) | `test_live_maps_vendor_payload_to_canonical`; `test_only_freshness_differences_are_not_state_changes`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `DataFreshness.is_stale` | Not supplied by provider; local TTL cache marks stale fallback | Defaults false; ephemeral cache overlay and intentionally not persisted as source data; match hero shows stale state when true | `test_live_list_stale_fallback_bounded_at_300_seconds`; `test_get_match_live_detail_cached_60_seconds_and_stale_marked`; `frontend/components/match-page.test.tsx`; verified |
| `DataFreshness.age_seconds` | Not supplied by provider; local TTL cache computes age | Nonnegative integer, defaults 0; ephemeral cache overlay, not persisted; used in stale label | `test_live_list_stale_fallback_bounded_at_300_seconds`; `test_upcoming_list_fresh_600_and_stale_bounded_at_1800`; verified |
| `Player.id` | `first_player_key` / `second_player_key` → provider-scoped identity repository → internal ID | Player and match rows store internal IDs; score/PBP/server references use them; REST/SSE keep internal ID | `test_internal_ids_are_stable_and_reversible_across_calls`; `test_vendor_fields_never_leak_into_canonical_output`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `Player.name` | `event_first_player` / `event_second_player`; directory/profile hydration upgrades abbreviations | Player row stores preferred English name; reducer protects fuller hydrated name; hero renders English name | `test_feed_updates_preserve_hydrated_player_metadata`; `test_profile_metadata_change_advances_version`; `test_live_maps_vendor_payload_to_canonical`; verified |
| `Player.localized_name` | Not in API-Tennis match row; local alias directory enrichment | Player row; reducer retains hydrated value on sparse frames and publishes corrections; current match hero does not show it | `test_feed_updates_preserve_hydrated_player_metadata`; `test_localized_name_correction_advances_version`; repository player conflict update; verified |
| `Player.country_code` | API-Tennis player profile/standings country names use explicit tennis aliases then ISO registry lookup; a match row alone does not supply country | Canonical ISO alpha-3 persisted in the player directory and hydrated into matches; genuinely unrecognized names remain null; `World` maps to `world` | `test_metadata_normalizers_map_country_names_and_leave_unknown_values_unmapped`; `test_current_ranking_countries_map_to_iso_alpha3`; verified |
| `Player.country_alpha2` | Derived from canonical `country_code` through the ISO registry; never independently supplied or persisted | Additive REST/SSE presentation field used to localize country names and construct FlagCDN URLs for all ISO countries; null for `world` or unknown codes | `test_player_derives_alpha2_for_complete_nationality_presentation`; computed/round-trip model test; verified |
| `Player.ranking` | Current ATP/WTA `get_standings.place`; season-specific profile `stats[].rank` is not current singles rank | Ranking directory snapshot is authoritative; snapshot hydration/reducer replaces and clears stale ranks; hero shows `#rank` or unavailable | `test_get_player_does_not_use_season_rank_as_current_ranking`; `test_authoritative_rankings_replace_and_clear_stale_values`; `test_realtime_reconcile_replaces_stale_ranks_and_keeps_them_on_sparse_frame`; verified in code/tests; running-service check is a separate gate |
| `Tournament.id` | `tournament_key`; if absent, `event_key` is a per-match fallback, not a claim that two such matches share a tournament | Provider-scoped identity → internal ID; tournament row and response | `test_internal_ids_are_stable_and_reversible_across_calls`; `test_unknown_server_and_missing_tournament_metadata_stay_explicit`; verified; missing source key limits cross-match grouping |
| `Tournament.name` | `tournament_name`, trimmed; blank becomes `Unknown tournament` | Tournament row; hero/header display | `test_live_maps_vendor_payload_to_canonical`; `test_unknown_server_and_missing_tournament_metadata_stay_explicit`; verified |
| `Tournament.tour` | `event_type_type` explicitly beginning `Atp ` or `Wta ` | Stored as `atp`/`wta`; prediction requires a recognized matching tour, otherwise abstains | `test_match_mapping_preserves_recognized_tour_identity`; `test_prematch_with_unknown_tour_abstains_instead_of_using_atp`; `test_prematch_with_tour_circuit_mismatch_abstains`; bug fixed and verified |
| `Tournament.circuit` | `event_type_type` family classifier | Canonical ATP/WTA/Challenger/ITF/other; persisted and used by catalog filters; no exact tournament tier inferred | `test_classifies_reliable_event_types`; `test_match_mapping_preserves_recognized_tour_identity`; verified |
| `Tournament.gender` | `event_type_type` Men/Women/Mixed classifier; junior Boys/Girls remain unknown | Canonical enum; persisted and used by filters | `test_classifies_reliable_event_types`; verified |
| `Tournament.discipline` | `event_type_type` Singles/Doubles/Teams classifier | Canonical enum; persisted and used by filters; individual doubles member identity is outside current `Player` shape | `test_classifies_reliable_event_types`; verified; see supplier-only limits below |
| `SetScore.number` | `scores[].score_set`; invalid/nonpositive value falls back to one-based response order | Included in score JSON/REST/SSE; hero uses positive set headings | `test_live_maps_vendor_payload_to_canonical`; `test_invalid_set_numbers_fall_back_to_response_order`; `test_set_score_number_is_one_based`; bug fixed and verified |
| `SetScore.player1_games` | `scores[].score_first` parsed as trimmed nonnegative integer | Missing/invalid/negative → null; displayed as unavailable, never guessed | `test_live_maps_vendor_payload_to_canonical`; `test_set_game_scores_trim_whitespace_and_reject_negative_values`; bug fixed and verified |
| `SetScore.player2_games` | `scores[].score_second` parsed as trimmed nonnegative integer | Missing/invalid/negative → null; displayed as unavailable, never guessed | `test_live_maps_vendor_payload_to_canonical`; `test_set_game_scores_trim_whitespace_and_reject_negative_values`; bug fixed and verified |
| `MatchScore.sets_won` | `event_final_result` integer pair; missing/unparseable → null | REST/SSE and scoreboards; player-history UI shows the explicitly labeled set-count fallback (e.g. `2–0 盘`) only when detailed game scores are absent; never synthesizes 0–0 | `test_missing_final_set_count_stays_unknown_in_match_and_pbp`; `test_finished_result_keeps_set_count_when_vendor_has_no_set_rows`; `frontend/lib/player-view-models.test.ts`; bug fixed and verified |
| `MatchScore.sets` | `scores[]` mapped to ordered `SetScore` rows | Empty or rows with no game values mean detailed set scores were not reported; UI prefers real game rows, then falls back to `sets_won`, while retaining live points | `test_live_maps_vendor_payload_to_canonical`; `test_finished_result_keeps_set_count_when_vendor_has_no_set_rows`; `frontend/lib/player-view-models.test.ts`; bug fixed and verified |
| `MatchScore.points` | `event_game_result` parsed as a pair of tokens | Missing/unparseable → `(null, null)`; live hero shows only supplied tokens | `test_live_maps_vendor_payload_to_canonical`; `parse_point_pair`; verified; unsupported token semantics remain source-limited |
| `MatchScore.is_tiebreak` | API-Tennis documents no marker/encoding; legacy LiveTennis can provide nullable boolean | API-Tennis maps to null and never infers tie-break from score; LiveTennis maps explicit bool | `test_missing_provider_tiebreak_marker_stays_unknown`; `test_live_maps_vendor_payload_to_canonical`; `test_livetennis_provider_maps_live_payload`; unavailable from current P2 provider |
| `LiveMatchState.current_set_number` | Exact live `event_status` form `Set N`; not inferred from returned score-row count; any other/missing status → null | State JSON is persisted and carried by REST/SSE; hero and score-progress badge show the official set only when present; set columns are highlighted only on an exact number match | Official REST example `event_status: Set 1`; `test_live_maps_vendor_payload_to_canonical`; `test_live_state_does_not_infer_current_set_from_score_rows`; PG round-trip; two-row/third-set UI regression; bug fixed |
| `LiveMatchState.score` | Derived from `event_final_result`, `event_game_result`, `scores[]`; a live event may have empty set rows | JSON in match-state snapshot; REST/SSE; Home/Match hero; no fabricated set heading | `test_live_maps_vendor_payload_to_canonical`; `test_snapshot_of_upcoming_match_is_version_zero_with_declared_gaps`; no-set UI regression; bug fixed and verified |
| `LiveMatchState.server_player_id` | `event_serve` (`First Player`/`Second Player`) → internal side ID; unknown token → null | Persisted in state JSON; REST/SSE; hero marks neither side when absent/invalid | `test_live_maps_vendor_payload_to_canonical`; `test_unknown_server_and_missing_tournament_metadata_stay_explicit`; frontend unknown-server regression; verified |
| `LiveMatchState.state_version` | Internal reducer sequence, not vendor value | Persisted in row + state JSON; SSE frame ID/version matches REST snapshot | `test_initial_reduction_assigns_version_one_and_reports_changes`; `test_reduction_snapshot_stays_version_consistent`; REST/SSE parity tests; verified |
| `LiveMatchState.connection_status` | Internal worker/feed health, not match result status | State JSON + persistence column; controls realtime UI connection notice | `test_reconnect_reconciles_via_rest_before_later_ws_deltas`; `test_terminal_state_closes_immediately_and_publishes_match_ended`; verified |
| `LiveMatchState.last_event_at` | No documented source event timestamp; never populated from provider data | Nullable; API-Tennis source time unavailable; `MatchSnapshot.as_of` / `DataFreshness.observed_at` are the actual local observation clocks | Official API/WS docs; unavailable by provider contract |
| `LiveMatchState.as_of` | No separate supplier timestamp; not populated by current adapters | Nullable; top-level `MatchSnapshot.as_of` is the persisted/transported snapshot observation time | Official API/WS docs; unavailable as a distinct provider field |
| `Match.id` | `event_key` → provider-scoped identity repository → internal ID | `MatchRow.id`; REST path, SSE match ID and frontend href | `test_internal_ids_are_stable_and_reversible_across_calls`; REST/ready/delta contract tests; verified |
| `Match.status` | `event_status` plus `event_live`; explicit terminal state takes precedence | `MatchRow.status`; page chooses scheduled/live/finished presentation | `test_event_live_flag_fills_missing_live_status_but_terminal_wins`; verified |
| `Match.players` | First/second player keys and names; side order is preserved | Pair order controls all score/PBP values; persistence `player1_id/player2_id`; doubles are composite-name sides, not individual pair members | `test_live_maps_vendor_payload_to_canonical`; PBP server-side and stat-side assertions; verified; see doubles limitation below |
| `Match.tournament` | Tournament key/name/type metadata | Tournament row stores fields; response embeds canonical Tournament | `test_live_maps_vendor_payload_to_canonical`; taxonomy tests; PG round-trip test; verified |
| `Match.scheduled_at` | `event_date` + `event_time`, request asks for `timezone=GMT` | ISO UTC datetime; missing date/time/invalid value → null; UI formats Beijing time (`Asia/Shanghai`) | `test_live_request_uses_method_timezone_and_key`; `test_live_maps_vendor_payload_to_canonical`; `formatAsOf`/time view-model tests; verified |
| `Match.round` | `tournament_round`; official examples use empty string/null | Blank normalizes to null; DB stores nullable text; page shows unavailable | Official REST/WS examples; `test_blank_provider_round_maps_to_unavailable_value`; bug fixed and verified |
| `Match.surface` | Separate `get_draw` `tournament_surface`; explicit alias map | Canonical hard/clay/grass or null; persisted; hero displays mapped surface | `test_match_snapshot_enriches_surface_from_official_draw_metadata`; `test_match_snapshot_keeps_match_when_draw_metadata_is_unavailable`; verified |
| `Match.indoor` | API-Tennis match/WS docs and DTO supply no indoor field; legacy provider may include it | API-Tennis null; persisted nullable; UI says unavailable | Official REST/WS schemas; `test_live_list_maps_vendor_payload_to_canonical`; unavailable from current P2 provider |
| `Match.format` | API-Tennis match/WS docs and DTO supply no match format; legacy provider may include it | API-Tennis null; persisted nullable; UI says unavailable | Official REST/WS schemas; `test_live_list_maps_vendor_payload_to_canonical`; unavailable from current P2 provider |
| `Match.live_state` | Nested current score/feed state; null only when no canonical live state exists | State stored in snapshot row; all score UI reads this object | `test_live_maps_vendor_payload_to_canonical`; REST/SSE exact snapshot contract; PG round-trip; verified |
| `Match.winner_player_id` | `event_winner` first/second token → canonical player ID only for finished matches | Nullable match column; hero marks winner; live/cancelled rows never claim winner | `test_get_recent_results_uses_player_window_and_terminal_filter`; `test_non_finished_match_does_not_claim_event_winner`; bug fixed and verified |
| `Match.freshness` | API-Tennis local receive time | Provider and source/observation timestamps survive PostgreSQL reconstruction; stale/age remain cache overlays; REST/SSE returns canonical freshness | `test_live_maps_vendor_payload_to_canonical`; `test_only_freshness_differences_are_not_state_changes`; cache stale tests; `test_load_snapshot_rebuilds_the_canonical_view`; bug fixed |
| `DataQuality.capability` | Internal capability key (`point_by_point`, `statistics`, `momentum`, `point_winner`) | Stored in snapshot `quality` JSON; REST/SSE; UI uses relevant capability/status to explain availability | `test_snapshot_quality_declares_available_and_missing_capabilities`; `test_computed_recent_control_is_declared_available`; verified |
| `DataQuality.status` | Mapper/reducer declares available/partial/unavailable/stale; statistics are partial if any metric has only one player side | Stored in snapshot `quality` JSON; REST/SSE; statistic rows show partial/stale labels | `test_snapshot_quality_declares_available_and_missing_capabilities`; `test_statistics_capability_is_partial_when_only_one_side_is_returned`; `test_empty_statistics_update_retains_last_values_as_stale`; bug fixed and verified |
| `DataQuality.provider` | `api_tennis` for source capabilities; `tennix` for computed momentum | Stored in snapshot `quality` JSON; internal provenance, not vendor DTO | `test_computed_recent_control_is_declared_available`; provider snapshot quality tests; verified |
| `DataQuality.reason` | Mapper reasons include `not_reported`/`not_computed`; reducer adds explicit sparse/stale reasons | Nullable string, persisted in snapshot JSON and REST/SSE; unknown reasons are not invented | `test_snapshot_quality_declares_available_and_missing_capabilities`; `test_empty_statistics_update_retains_last_values_as_stale`; verified |
| `DataQuality.observed_at` | Local clock when capability assessment is made or changed | UTC-aware; persisted in snapshot `quality` JSON; distinct from supplier event time | `test_snapshot_quality_declares_available_and_missing_capabilities`; `test_quality_change_emits_quality_updated`; integration round-trip in `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `PointEvent.id` | Internal ID based on match + response sequence; reducer re-IDs collisions deterministically | Point row primary key; PBP React key | `test_inserted_supplier_point_does_not_reuse_an_existing_point_id`; `test_resequenced_existing_point_does_not_reuse_its_old_row_id`; verified |
| `PointEvent.match_id` | Internal `Match.id` | DB owner relation; snapshot validator rejects points from another match | `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `PointEvent.sequence` | Flattened provider PBP order, then reducer-contiguous sequence | DB ordered by sequence; MatchPoints timeline and recent-ten display | `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; `test_dropped_tail_is_rebuilt_from_first_difference`; verified |
| `PointEvent.set_number` | Valid provider PBP game `set_number` is parsed as a positive integer; when absent/invalid, local grouping policy inherits the last known value (starts at 1) | Persisted numeric; timeline group | Official WS sample; `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; `test_nonpositive_pbp_numbers_fall_back_to_positive_sequence_values`; valid-value mapping verified; fallback is internal policy, not documented provider semantics |
| `PointEvent.game_number` | Valid provider `number_game` is parsed as a positive integer; when absent/invalid, local grouping policy restarts at 1 and increments within each set | Persisted numeric; timeline group | `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; `test_missing_game_numbers_restart_at_one_for_each_set`; `test_nonpositive_pbp_numbers_fall_back_to_positive_sequence_values`; valid-value mapping verified; fallback is internal policy, not documented provider semantics |
| `PointEvent.point_number` | Valid provider `number_point` is parsed as a positive integer; local identity policy fills missing/invalid values with the next positive number and bumps duplicates within a game | Persisted numeric identity component | Official WS sample; `test_snapshot_normalizes_duplicate_vendor_point_numbers`; `test_nonpositive_pbp_numbers_fall_back_to_positive_sequence_values`; valid-value mapping verified; missing/duplicate correction semantics are not documented by provider |
| `PointEvent.server_player_id` | PBP game `player_served` first/second token → internal side ID | Internal player ID; timeline and intelligence packet; unknown token → null | `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; `test_unknown_server_and_missing_tournament_metadata_stay_explicit`; verified |
| `PointEvent.winner_player_id` | Derived from one-side increase of preceding/current point score; supported normal-point tokens only | Null plus `winner_indeterminate` quality when not derivable; page's recent-ten and momentum omit indeterminate winners | `test_indeterminate_points_keep_quality_and_no_winner`; `test_advantage_reset_assigns_point_to_player_who_removed_advantage`; tie-break scores remain unsupported/unknown |
| `PointEvent.score_before` | Previous score within same PBP game; first point seeded `0-0` | Nested MatchScore JSON; timeline tooltip | `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; verified for fixture convention |
| `PointEvent.score_after` | PBP point `score` pair | Nested MatchScore JSON; timeline and tooltips | `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; normal supported tokens only |
| `PointEvent.is_break_point` | PBP `break_point`; API-Tennis value is nullable undocumented text, so current mapping always returns null | Nullable bool persisted to DB/REST/SSE; UI badge/intelligence key-point only when true | Official docs show null but don't define its meaning or non-null tokens; `test_null_point_flags_remain_unknown`; `test_unrecognized_point_flag_text_remains_unknown`; migration `0007` converts legacy API-Tennis booleans to unknown; unavailable by provider semantics |
| `PointEvent.is_set_point` | PBP `set_point`; API-Tennis value is nullable undocumented text, so current mapping always returns null | Nullable bool persisted to DB/REST/SSE; UI badge/intelligence key-point only when true | Official docs show null but don't define its meaning or non-null tokens; `test_null_point_flags_remain_unknown`; `test_unrecognized_point_flag_text_remains_unknown`; migration `0007` converts legacy API-Tennis booleans to unknown; unavailable by provider semantics |
| `PointEvent.is_match_point` | PBP `match_point`; API-Tennis value is nullable undocumented text, so current mapping always returns null | Nullable bool persisted to DB/REST/SSE; UI badge/intelligence key-point only when true | Official docs show null but don't define its meaning or non-null tokens; `test_null_point_flags_remain_unknown`; `test_unrecognized_point_flag_text_remains_unknown`; migration `0007` converts legacy API-Tennis booleans to unknown; unavailable by provider semantics |
| `PointEvent.observed_at` | Local ingestion time, shared by points in one mapped frame | DB timestamp; UTC-aware; used as PBP observation time | `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `PointEvent.provider` | Mapper constant `api_tennis` | DB/REST/SSE provenance | `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; verified |
| `PointEvent.source_fingerprint` | Hash of canonicalized source set/game/point/score/server/flags | DB dedupe/correction input, not UI | `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; correction/replay tests in reducer |
| `PointEvent.revision` | Internal revision counter starts at 1 and advances on correction | Point row + append-only revision history; UI doesn't expose revision | `test_point_correction_starts_recompute_at_changed_sequence`; `test_second_reduction_appends_point_and_records_revision`; verified |
| `PointEvent.quality` | Internal winner-confidence record (`winner_indeterminate`) | Nullable nested quality JSON; forwarded in REST/SSE; current page does not render it | `test_indeterminate_points_keep_quality_and_no_winner`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `MatchStatistic.match_id` | Internal match ID assigned by mapper | DB row; snapshot validator rejects cross-match statistics | `test_snapshot_maps_22_stat_catalog_and_ignores_unmappable`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `MatchStatistic.name` | Vendor `stat_name` → explicit 22-entry `STAT_NAME_MAP`; unknown dropped | DB enum/text, REST/SSE; frontend `STAT_META` label/group | `test_stat_catalog_covers_each_canonical_metric_with_its_unit`; `test_snapshot_maps_22_stat_catalog_and_ignores_unmappable`; verified |
| `MatchStatistic.period` | Vendor `stat_period`: `match` or positive set number; malformed/zero/negative is dropped | Stored as `match` / `set:N`; UI labels full match or set | `test_snapshot_maps_22_stat_catalog_and_ignores_unmappable`; `test_invalid_stat_periods_are_not_exposed`; bug fixed |
| `MatchStatistic.player1_value` | Only matching first-side `player_key`; finite, nonnegative `stat_value` parsed numerically | DB float nullable; MatchStatisticsCard left value; no zero fabricated for missing row | `test_snapshot_maps_22_stat_catalog_and_ignores_unmappable`; `test_invalid_stat_values_are_not_exposed`; integration round-trip; verified |
| `MatchStatistic.player2_value` | Only matching second-side `player_key`; unknown keys are discarded instead of creating an empty partial row | DB float nullable; MatchStatisticsCard right value; null displays unavailable | `test_snapshot_maps_22_stat_catalog_and_ignores_unmappable`; `test_statistic_for_unknown_player_is_not_exposed`; verified; unknown-key bug fixed |
| `MatchStatistic.unit` | Explicit canonical unit in `STAT_NAME_MAP` | Persisted/API value; frontend uses provider canonical unit before static preview metadata | `test_snapshot_maps_22_stat_catalog_and_ignores_unmappable`; `MatchStatisticsCard uses the canonical unit for total points and games won`; bug fixed |
| `MatchStatistic.provenance` | Provider rows → `provider`; current P2 does not synthesize statistic rows from PBP | DB enum; UI doesn't show provenance | `test_snapshot_maps_22_stat_catalog_and_ignores_unmappable`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `MatchStatistic.availability` | Both sides populated → available; one side → partial; old retained rows → stale | DB/API; UI shows partial/stale badge and null-side unavailable copy | `test_statistics_capability_is_partial_when_only_one_side_is_returned`; `test_sparse_statistics_update_refreshes_present_metric_only`; integration round-trip; verified |
| `MatchStatistic.as_of` | Local observation clock at mapping; sparse reducer refreshes current metric time | DB/API; UI uses newest metric time and stale rows keep their own timestamp | `test_sparse_statistics_update_refreshes_present_metric_only`; `test_unchanged_fresh_metric_advances_its_observation_time`; `MatchStatisticsCard` freshness tests; verified |
| `MomentumObservation.match_id` | Internal match ID from input snapshot | DB ownership; snapshot validator rejects cross-match observations; match page momentum card | `test_initial_reduction_computes_versioned_recent_control`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `MomentumObservation.point_sequence` | Sequence of the determinate point just processed | DB ordered by sequence; chart x axis and key-point join | `test_initial_reduction_computes_versioned_recent_control`; `test_observations_carry_algorithm_version_and_leader`; verified |
| `MomentumObservation.state_version` | Reducer version used by computation | DB/API; internal consistency invariant, not shown as chart label | `test_initial_reduction_computes_versioned_recent_control`; `test_reduction_snapshot_stays_version_consistent`; PG round-trip; verified |
| `MomentumObservation.algorithm_version` | `RecentControlEngine` algorithm ID | DB/API; UI calls it a derived control index, not a provider statistic | `test_observations_carry_algorithm_version_and_leader`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `MomentumObservation.value` | Derived control score constrained to [-100,100] | DB/API; chart y value; never supplied by API-Tennis | `test_output_is_bounded`; `test_initial_reduction_computes_versioned_recent_control`; verified |
| `MomentumObservation.leader_player_id` | Positive score selects focal player, negative score selects the other side when determinable, zero → null | DB/API; UI renders name or neutral copy | `test_observations_carry_algorithm_version_and_leader`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `MomentumObservation.is_provisional` | Derived engine flag: true until six determinate points have contributed | DB/API; UI provisional badge/copy | `test_fewer_than_six_determinate_points_is_provisional`; `MatchMomentumCard` provisional test; verified |
| `MomentumObservation.as_of` | Observation time of the last input point used, not supplier event time | DB/API; UI labels the latest observation's own timestamp | `test_computed_recent_control_is_declared_available`; `MatchMomentumCard uses the latest momentum observation time instead of the snapshot time`; PG round-trip; verified |
| `MomentumObservation.input_summary` | Engine-generated compact calculation diagnostic, not vendor text | DB/API and bounded match-intelligence packet; not shown in page chart | `test_input_summary_uses_pre_point_serve_history`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |
| `MatchSnapshot.match` | Canonical match aggregate | Top-level REST/SSE; Match page | `test_snapshot_endpoint_returns_full_snapshot`; `test_rest_snapshot_matches_initial_sse_ready_snapshot`; `test_persisted_snapshot_tracks_sse_delta_version`; verified |
| `MatchSnapshot.points` | Mapped/reduced point collection | DB rows; REST/SSE; MatchPointsTimeline | `test_snapshot_maps_points_with_sequence_flags_and_winner_rules`; empty means not reported |
| `MatchSnapshot.statistics` | Mapped/reduced statistic collection | DB rows; REST/SSE; MatchStatisticsCard | `test_snapshot_maps_22_stat_catalog_and_ignores_unmappable`; empty means not reported |
| `MatchSnapshot.momentum` | Reducer-derived; empty until valid input points | DB rows; REST/SSE; MatchMomentumCard | `test_initial_reduction_computes_versioned_recent_control`; empty collection plus quality explains unavailable |
| `MatchSnapshot.quality` | Capability and point quality declarations; stats partial/stale reflects constituent metrics | DB JSON; REST/SSE; stats/momentum availability UX and intelligence packet | `test_snapshot_quality_declares_available_and_missing_capabilities`; `test_statistics_capability_is_partial_when_only_one_side_is_returned`; REST/SSE and PG round-trip tests; bug fixed and verified |
| `MatchSnapshot.state_version` | Internal reducer version, equals nested live-state version or 0 | Snapshot validator enforces invariant; SSE frame id | `test_reduction_snapshot_stays_version_consistent`; `test_rest_initial_snapshot_persists_before_stream_envelopes`; verified |
| `MatchSnapshot.as_of` | Local snapshot observation time, refreshed when the canonical snapshot is created | UTC-aware API/SSE envelope; distinct from vendor event time and per-stat/per-momentum times | `test_snapshot_of_upcoming_match_is_version_zero_with_declared_gaps`; `test_rest_snapshot_matches_initial_sse_ready_snapshot`; `test_persisted_snapshot_tracks_sse_delta_version`; `test_load_snapshot_rebuilds_the_canonical_view`; verified |

## All 22 statistic names

Each mapping comes from `api_tennis.py::STAT_NAME_MAP`; official REST docs show `stat_period`, `stat_name`, `stat_value`, `stat_won`, and `stat_total`. Canonical value comes from finite, nonnegative `stat_value`; percentages above 100 are rejected. The sanitized fixture verifies `72%` remains `72.0 percent` even though `stat_won=26` and `stat_total=36`; those counters and `stat_type` remain vendor-only because their general semantics are not defined for every metric and the canonical model represents one value/unit per side. Full mapping and UI inventory are exercised by `test_stat_catalog_covers_each_canonical_metric_with_its_unit`, `test_snapshot_maps_22_stat_catalog_and_ignores_unmappable`, and `MatchStatisticsCard`'s 22-label test. Canonical provider `unit` drives display.

| Canonical name | Vendor `stat_name` | Unit | UI label/group | Current status |
|---|---|---|---|---|
| `aces` | `Aces` | count | ACE 球 / 发球 | verified |
| `double_faults` | `Double Faults` | count | 双误 / 发球 | verified |
| `first_serve_percentage` | `1st serve percentage` | percent | 一发成功率 / 发球 | verified |
| `first_serve_points_won` | `1st serve points won` | percent | 一发得分率 / 发球 | verified |
| `second_serve_points_won` | `2nd serve points won` | percent | 二发得分率 / 发球 | verified |
| `service_points_won` | `Service Points Won` | percent | 发球得分率 / 发球 | verified |
| `service_games_won` | `Service Games Won` | percent | 发球局胜率 / 发球 | verified |
| `break_points_saved` | `Break Points Saved` | percent | 破发点挽救率 / 关键分 | verified |
| `break_points_converted` | `Break Points Converted` | percent | 破发点转化率 / 关键分 | verified |
| `return_points_won` | `Return Points Won` | percent | 接发得分率 / 接发 | verified |
| `first_return_points_won` | `1st Return Points Won` | percent | 一发接发得分率 / 接发 | verified |
| `second_return_points_won` | `2nd Return Points Won` | percent | 二发接发得分率 / 接发 | verified |
| `return_games_won` | `Return Games Won` | percent | 接发局胜率 / 接发 | verified |
| `winners` | `Winners` | count | 制胜分 / 制胜与失误 | verified |
| `unforced_errors` | `Unforced Errors` | count | 非受迫性失误 / 制胜与失误 | verified |
| `net_points_won` | `Net Points Won` | percent | 上网得分率 / 制胜与失误 | verified |
| `total_points_won` | `Total Points Won` | percent | 总得分 / 总计 | bug fixed: display now follows canonical unit |
| `total_games_won` | `Total Games Won` | percent | 总赢局 / 总计 | bug fixed: display now follows canonical unit |
| `match_points_saved` | `Match Points Saved` | count | 赛点挽救 / 关键分 | verified |
| `average_first_serve_speed` | `Average 1st Serve Speed` | km/h | 一发平均速度 / 体能 | verified |
| `average_second_serve_speed` | `Average 2nd Serve Speed` | km/h | 二发平均速度 / 体能 | verified |
| `distance_covered` | `Distance Covered` | m | 跑动距离 / 体能 | verified |

## Confirmed defects fixed and verified

1. Empty set rows no longer produce “第 0 盘” or a fabricated score; current point score remains visible. `frontend/components/match-page.test.tsx`.
2. All 22 statistic labels/groups are covered; percentage values render with `%`, including total points/games. `frontend/components/match/match-statistics.test.tsx`.
3. ATP/WTA tour identity survives mapping and prediction fails closed for unknown/mismatched tour. `test_match_mapping_preserves_recognized_tour_identity`, `test_prematch_with_unknown_tour_abstains_instead_of_using_atp`, `test_prematch_with_tour_circuit_mismatch_abstains`.
4. Statistics capability is `partial` when any reported metric is one-sided; invalid values/periods and rows for unknown player keys are dropped. `test_statistics_capability_is_partial_when_only_one_side_is_returned`, `test_invalid_stat_values_are_not_exposed`, `test_invalid_stat_periods_are_not_exposed`, `test_statistic_for_unknown_player_is_not_exposed`.
5. Stored snapshots preserve the original freshness provider/source timestamp instead of relabeling it as PostgreSQL. Migration `0008`; `test_load_snapshot_rebuilds_the_canonical_view`.
6. A live page no longer infers the current set from the count of score rows; exact provider set status is persisted and shown, or displayed as unavailable. `test_live_maps_vendor_payload_to_canonical`; `test_live_state_does_not_infer_current_set_from_score_rows`; PostgreSQL round-trip and `frontend/components/match-page.test.tsx`.
7. Percent-valued replay statistics use percentage-shaped values rather than count-like numbers. `test_replay_materializes_canonical_identity_and_full_statistics`.
8. Migration `0008` refuses to drop non-null persisted freshness during downgrade; the PostgreSQL migration-cycle check confirms the version, column, and data remain intact on refusal. `test_freshness_migration_refuses_to_drop_persisted_values`.

## Provider-only fields and explicit limits

The API-Tennis DTO accepts forward-compatible vendor fields, but the canonical layer intentionally does not pass them through. The official REST examples include `first_player_dp1_key` / `first_player_dp2_key` and second-side equivalents for doubles, tournament season/qualification fields, and statistic `stat_type`, `stat_won`, `stat_total`. WebSocket PBP game objects also include `serve_winner`, `serve_lost`, and a game-level `score`. These are not represented in the current single-player-per-side `Match` / point-level statistic model; no meaning is inferred from them. API-Tennis single/doubles classification is retained, but doubles partner identities are not independently modeled.

Other intentionally unmodeled fields include vendor logos/images and supplier external IDs in public output. External IDs remain inside the provider identity mapping. API-Tennis has no match-row indoor/format, player-country alpha-3, source event timestamp, or distinct tie-break flag in the documented REST/WS schema. Separate profile/standings calls can supply country/rank metadata; known tennis-specific country aliases are explicit and ordinary names resolve through the ISO country registry. Unknown names remain null.

### Unknown provider semantics (not guessed)

- Tie-break marker/score representation and whether 7-6 score rows encode tie-break points.
- Meaning and truth values of nullable `break_point` / `set_point` / `match_point` values (`First Play` is present in a sanitized fixture; official docs show null but do not define either null or non-null tokens). All remain `null`, never inferred as false/true.
- Whether omitted WebSocket arrays/fields mean “unchanged” or “cleared”; official docs say updates are pushed but do not define merge semantics. Reducer retention of sparse statistics is an explicit local policy: omitted metrics stay with their original time and become stale; newly received metrics refresh independently. Regression: `test_empty_statistics_update_retains_last_values_as_stale`, `test_sparse_statistics_update_refreshes_present_metric_only`.
- API-Tennis provides no source event/update timestamp; local `observed_at` / `as_of` are not substitutes and are labeled as local observation clocks.

## T96 Player and Historical Results Field Matrix

Status: complete. This appendix covers the player-directory and bounded-history models not in the T95 match-field inventory. Product calendar dates and user-facing date/time labels use Beijing (`Asia/Shanghai`); stored timestamps remain timezone-aware UTC.

### Rankings

`get_standings(event_type=ATP|WTA)` is mapped to `RankingEntry`, atomically saved by `PlayerDirectorySync`, and read from the latest directory snapshot. Profile, player search, ranking pages, and match hydration use that same current snapshot rather than `get_players.stats[].rank`.

| Field | Source and mapping | Storage / REST / UI semantics | Evidence |
|---|---|---|---|
| `RankingEntry.player.id` | Provider `player_key` mapped through the internal identity repository | Stable internal ID in directory and API links; vendor key is not public | `test_get_rankings_reuses_internal_ids_across_calls`; verified |
| `RankingEntry.player.name` | Standings `player` label; profile enrichment may use `player_full_name` then `player_name` | English display name; richer profile data does not replace the internal identity | `test_get_rankings_maps_standings_to_internal_entries`; verified |
| `RankingEntry.player.localized_name` | Offline alias directory | Optional Chinese secondary label in rankings/search/profile; not from standings | directory alias tests; verified |
| `RankingEntry.player.country_code` | Standings `country` name through explicit tennis aliases and ISO registry lookup | ISO alpha-3; unknown/unmapped values stay null; ranking table and player cards | `test_current_ranking_countries_map_to_iso_alpha3`; verified |
| `RankingEntry.player.country_alpha2` | Derived from `country_code` | ISO alpha-2 used for localized country name and flag presentation; not separately stored | `test_player_derives_alpha2_for_complete_nationality_presentation`; verified |
| `RankingEntry.player.ranking` | Mirrored from canonical snapshot `rank` | Nested summary rank must match `RankingEntry.rank`; no profile-season rank fallback | `test_rankings_page_nested_player_rank_matches_snapshot_rank`; verified |
| `RankingEntry.tour` | Requested `event_type` (`ATP`/`WTA`) | Snapshot partition key and REST field | ATP/WTA provider parameter tests; verified |
| `RankingEntry.rank` | `place`, accepted only as positive integer | Current ordinal from latest standings; tied rank keeps both players while snapshot key remains unique | `test_get_rankings_skips_rows_without_usable_key_name_rank_or_points`; repository tie tests; verified |
| `RankingEntry.points` | `points`, accepted only as non-negative integer | Tour ranking points; displayed as points, not inferred from profile stats | `test_get_rankings_maps_standings_to_internal_entries`; verified |
| `RankingEntry.movement` | Vendor `movement` is ignored because docs do not define comparison interval; repository compares adjacent retained snapshots | `up`/`down`/`same` is derived locally; `unknown` if no comparable prior snapshot | `test_get_rankings_does_not_trust_undocumented_vendor_movement`; `test_snapshot_movement_is_derived_from_previous_ordinal_rank`; verified |
| `RankingEntry.ranking_date` | Snapshot capture instant converted to configured product timezone before taking date | Beijing calendar date for the standings snapshot; participates in snapshot identity | `test_ranking_snapshot_date_uses_beijing_calendar`; bug fixed |
| `RankingEntry.fetched_at` | Local injected observation clock | Aware UTC instant, not a provider publication timestamp; retained by ranking repository | provider/repository snapshot tests; verified |
| `RankingPage.tour` | Requested tour | Page title and tour partition | rankings API tests; verified |
| `RankingPage.page` | Requested 1-based page | API pagination metadata | rankings API tests; verified |
| `RankingPage.page_size` | Requested bounded page size | API pagination metadata | rankings API tests; verified |
| `RankingPage.total` | Filtered count from directory snapshot | Count after country filter, before page slicing | `test_get_rankings_filters_country_and_paginates`; verified |
| `RankingPage.entries` | Snapshot rows for requested page | Canonical entries consumed by rankings table and profile links | rankings API/UI tests; verified |
| `RankingPage.as_of` | Newest `fetched_at` from the unfiltered current tour snapshot | `null` when that tour has no snapshot; country/page filtering cannot fabricate request-time freshness. The directory formats valid snapshots in `Asia/Shanghai` and labels the displayed time “北京时间”; malformed values stay unavailable. | `test_empty_country_filter_keeps_the_rankings_snapshot_time`; `rankingsSnapshotNote` tests; `test_rankings_page_has_no_as_of_when_no_snapshot_exists`; bug fixed |
| `RankingPage.availability` | Directory snapshot availability | Separate from empty filters: a valid empty page is not a synthetic failure | rankings service/API tests; verified |

The T92 rule remains: a successful standings response must contain usable rows before it replaces a prior snapshot. API-Tennis has no documented source timestamp; `fetched_at` is local observation time. The vendor movement comparison interval is undocumented and is not trusted.

### Profile and season records

`get_players` maps identity/name/country/birth date/logo and only singles season rows. Current world ranking is joined from the latest standings snapshot by internal player ID; season-specific profile `rank` is deliberately not exposed as current rank.

| Field | Source and mapping | Storage / REST / UI semantics | Evidence |
|---|---|---|---|
| `PlayerProfileData.player.id` | Internal directory ID resolved to provider `player_key` only inside identity mapping | Stable `/players/{id}` route identity | profile provider/API tests; verified |
| `PlayerProfileData.player.name` | `player_full_name`, fallback `player_name` | English-primary display | `test_player_profile_maps_identity_fields_without_using_season_rank`; verified |
| `PlayerProfileData.player.localized_name` | Offline alias directory | Optional Chinese secondary display | localized player-profile tests; verified |
| `PlayerProfileData.player.country_code` | `player_country` via explicit tennis aliases then ISO registry lookup | Alpha-3 or null when unrecognized | provider country mapping tests; verified |
| `PlayerProfileData.player.ranking` | Latest directory standings projection, not `stats[].rank` | Null if no current snapshot entry; consistent with rankings/search/match | `test_player_profile_maps_identity_fields_without_using_season_rank`; verified |
| `PlayerProfileData.birth_date` | `player_bday` parsed strictly as `DD.MM.YYYY` | ISO date or null for absent/malformed input; age calculated in Beijing calendar | `test_player_profile_maps_identity_fields_without_using_season_rank`; frontend Beijing calendar test; verified |
| `PlayerProfileData.image_url` | `player_logo`, trimmed | Optional profile image URL; null if blank | profile provider/UI tests; verified |
| `PlayerProfileData.seasons` | `stats[]` filtered to `type=singles`, valid numeric season; ordered newest first | Only current season through prior four are queryable; doubles stats omitted | `test_season_stats_keep_blank_and_invalid_numbers_unavailable`; five-season service tests; verified |
| `PlayerSeasonRecord.season` | Numeric vendor season string | Season selector value | provider mapping tests; verified |
| `PlayerSeasonRecord.matches_won` | `matches_won` parsed as non-negative integer | Null for blank/malformed, never fabricated as zero | `test_season_stats_keep_blank_and_invalid_numbers_unavailable`; verified |
| `PlayerSeasonRecord.matches_lost` | `matches_lost` parsed as non-negative integer | Null for blank/malformed, never fabricated as zero | same provider test; verified |
| `PlayerSeasonRecord.titles` | `titles` parsed as non-negative integer | Null for blank/malformed | same provider test; verified |
| `PlayerSeasonRecord.hard` | `hard_won` and `hard_lost` | `null` only when both unavailable; each side may independently be null | same provider test; verified |
| `PlayerSeasonRecord.clay` | `clay_won` and `clay_lost` | Same explicit missing-value behavior | same provider test; verified |
| `PlayerSeasonRecord.grass` | `grass_won` and `grass_lost` | Same explicit missing-value behavior | same provider test; verified |
| `SurfaceRecord.won` | Surface wins parsed as non-negative integer | Null if unreported/invalid; the UI does not turn null into zero | provider and season-summary tests; verified |
| `SurfaceRecord.lost` | Surface losses parsed as non-negative integer | Null if unreported/invalid | provider and season-summary tests; verified |
| `PlayerProfileView.profile` | `PlayerProfileData` above | Profile header and season selector | profile REST/UI tests; verified |
| `PlayerProfileView.ranking` | Latest standings row for this player | Null without a current ranking snapshot; profile rank/points/movement/date | directory authority tests; verified |
| `PlayerProfileView.selected_season` | Requested season or current year in configured product timezone | Defaults to Beijing current year; valid window is current through prior four | `test_profile_default_season_uses_beijing_calendar_year`; bug fixed |
| `PlayerProfileView.season_record` | Exact matching row in `profile.seasons` | Null means no reported record for this season, not all-zero performance | season record tests; verified |
| `PlayerProfileView.current_match` | Singles live match first; otherwise earliest scheduled singles match | No doubles leakage; missing time sorts after dated fixtures, internal ID breaks ties | `test_profile_view_prefers_live_over_next`; `test_profile_view_selects_earliest_upcoming_match_not_provider_order`; `test_profile_current_match_excludes_doubles`; verified |

### Season result pages

The page endpoint loads finished fixtures for one requested calendar year, then applies singles, circuit, and outcome filters and sorts newest first. It uses a 20-row page and an in-process bounded cache; history is not persisted locally.

| Field | Source / meaning | Consumer and missing-value rule | Evidence |
|---|---|---|---|
| `PlayerResultPage.player` | Internal directory player; fallback is a singles result participant, otherwise an explicit “Unknown player” placeholder | Page heading; not used as a substitute for provider identity | profile results API tests; verified |
| `PlayerResultPage.season` | Requested year, validated against current Beijing year and previous four | Page selector and query parameter | `test_result_page_accepts_current_beijing_season_at_utc_year_boundary`; bug fixed |
| `PlayerResultPage.tiers` | Requested circuit filters; empty means all | Stacked-filter selection reflected in response | `test_result_page_paginates_and_filters`; verified |
| `PlayerResultPage.outcome` | `all` / `won` / `lost` request | Matches with unknown winner remain in `all`; excluded from both won/lost | `test_result_page_does_not_call_unknown_winner_a_loss`; bug fixed |
| `PlayerResultPage.page` | Requested 1-based page | Response pagination | results API tests; verified |
| `PlayerResultPage.page_size` | Fixed at 20 | Response pagination | results API tests; verified |
| `PlayerResultPage.total` | Count after singles/circuit/outcome filtering and before page slicing | Empty filtered set is `0`, independent of source availability | results API tests; verified |
| `PlayerResultPage.matches` | Finished canonical fixtures, singles only, sorted by scheduled timestamp descending | Rows include the full canonical Match DTO; result winner is shown as unknown if absent | `test_result_page_excludes_doubles_from_singles_history`; player results UI tests; verified |
| `PlayerResultPage.availability` | Successful empty query is available; a returned singles match without a canonical start timestamp makes the page partial | The page does not claim a full calendar-year result set when a returned date cannot be assigned exactly | `test_successful_empty_season_results_are_available_not_unavailable`; `test_result_page_marks_undated_matches_partial`; verified |

### Chat history and H2H

Chat `get_player_results` uses a 10-row provider cap for yesterday (last 30 days queried, then Beijing-yesterday filter) and a five-season on-demand cache for `last`/`recent`. `last` returns exactly one; `recent` returns the requested 1–10 newest finished matches. A yesterday result is `partial` when the provider response reaches the local cap, even if fewer than ten remain after date filtering. An undated returned singles result also makes the affected history scope `partial`, because its date/order cannot be determined reliably. Successful complete empty data is distinct from unsupported capability. No history rows are written to PostgreSQL.

| Model / field | Source and semantics | Surface / freshness | Evidence |
|---|---|---|---|
| `PlayerResults.player_id` | Internal resolved player ID | Chat structured history context; no provider ID | chat history tests; verified |
| `PlayerResults.scope` | `yesterday`, `last`, or `recent` | Determines calendar-day vs result-count semantics | chat acceptance/tool tests; verified |
| `PlayerResults.availability` | Unsupported yesterday capability → unavailable; capped yesterday response or an undated returned singles result → partial; otherwise a successful complete empty/small response → available | Structured Chat result and honest empty/warning copy; `last`/`recent` cannot claim completeness if any queried season includes an undated singles result | `test_yesterday_results_are_partial_when_provider_window_hits_fetch_limit`; `test_yesterday_history_is_partial_when_a_singles_result_is_undated`; `test_chat_history_is_partial_when_a_singles_result_is_undated`; bug fixed |
| `PlayerResults.matches` | Finished singles only, newest first, caller limit ≤10 | Chat structured results; yesterday dates interpreted in Beijing; `last`/`recent` stop after enough of the five seasons; doubles rows are filtered before satisfying the count limit | `test_yesterday_results_use_beijing_calendar`; `test_recent_history_excludes_newer_doubles_result`; service and chat history tests; bug fixed |
| `HeadToHeadResult.first_player_id` | Resolved internal ID, orientation preserved | API response identity | H2H API/service tests; verified |
| `HeadToHeadResult.second_player_id` | Resolved internal ID, orientation preserved | API response identity | H2H API/service tests; verified |
| `HeadToHeadResult.availability` | Unsupported → unavailable; any raw provider list reaching its 10-row cap before mapping, or any mapped list reaching 10 → partial; otherwise available | REST exposes availability; Chat carries it in metadata | `test_head_to_head_is_partial_when_any_collection_reaches_fetch_limit`; `test_head_to_head_reports_raw_cap_when_a_row_cannot_be_mapped`; `test_head_to_head_respects_provider_truncation_with_unmappable_rows`; verified |
| `HeadToHeadResult.head_to_head` | Null only when unsupported; otherwise canonical aggregate | REST envelope; Chat presents `meetings` as matches and includes the two recent-list counts in metadata | H2H REST/chat tests; verified |
| `HeadToHead.first_player_id` | Request orientation | Canonical identity, not provider key | H2H service tests; verified |
| `HeadToHead.second_player_id` | Request orientation | Canonical identity, not provider key | H2H service tests; verified |
| `HeadToHead.meetings` | API-Tennis `H2H` rows, mapped and locally bounded to 10 | REST nested matches; Chat structured meetings; source order is preserved because the docs do not specify it | H2H mapping/orientation tests; verified; source ordering undocumented |
| `HeadToHead.meetings_may_be_truncated` | True when the raw API-Tennis `H2H` array reaches the local cap, measured before invalid rows are discarded | Preserved in REST; contributes to aggregate `partial` availability | `test_head_to_head_reports_raw_cap_when_a_row_cannot_be_mapped`; verified |
| `HeadToHead.first_player_recent` | API-Tennis `firstPlayerResults`, mapped and locally bounded to 10 | REST nested matches; Chat exposes count only | H2H mapping tests; verified; source ordering undocumented |
| `HeadToHead.first_player_recent_may_be_truncated` | True when the raw `firstPlayerResults` array reaches the local cap, measured before mapping | Preserved in REST; contributes to aggregate `partial` availability | raw-cap provider test; verified |
| `HeadToHead.second_player_recent` | API-Tennis `secondPlayerResults`, mapped and locally bounded to 10 | REST nested matches; Chat exposes count only | H2H mapping tests; verified; source ordering undocumented |
| `HeadToHead.second_player_recent_may_be_truncated` | True when the raw `secondPlayerResults` array reaches the local cap, measured before mapping | Preserved in REST; contributes to aggregate `partial` availability | raw-cap provider test; verified |
| `HeadToHead.freshness.provider` | Constant `api_tennis` | Provenance label | H2H mapping tests; verified |
| `HeadToHead.freshness.source_updated_at` | Not returned by API-Tennis H2H | Always null; never substituted with local time | API-Tennis docs and H2H model tests; verified |
| `HeadToHead.freshness.observed_at` | Local aware UTC fetch clock | REST response observation time, not supplier event time | H2H mapping tests; verified |
| `HeadToHead.freshness.is_stale` | `DataFreshness` default false | H2H is in-process cached with bounded TTL and is not stale-served | cache/H2H tests; verified |
| `HeadToHead.freshness.age_seconds` | `DataFreshness` default 0 at observation | Not a supplier-reported age | freshness model/H2H tests; verified |

API-Tennis documents the three H2H result lists but does not state ordering or list limits. The local maximum of 10 is an application quota/response bound, not a provider guarantee. Tests cover every capped list independently. For `yesterday`, the source query is a recent-window request and the returned array can already be truncated before local date filtering; the `partial` badge communicates this uncertainty.

## T97 Market, Opportunity and Paper Presentation Matrix

This appendix traces the user-visible P3 fields from the public REST DTOs through the frontend view models to production consumers. It does not assert that a market has a model estimate or a liquid quote when the API returns null/unavailable. Labels intentionally describe the source and units instead of exposing provider or implementation terms.

| Field / group | API value and mapping | Consumer display / missing-value rule | Evidence / status |
|---|---|---|---|
| Market identity and match | `market_id`; optional internal `match_id`; provider question retained only as a fallback display name | Strictly matched rows link to the internal Match page. Unmatched markets remain visible by their real market name and are not given a fabricated player pair or destination. | `frontend/lib/p3-view-models.ts` `toMarketRow`; `markets-page.test.tsx` internal-link and market-only cases; verified |
| Tournament, tier, gender | `tournament_name`, canonical circuit `tier`, and `gender` | Human-readable Chinese labels; unknown values stay “其他比赛”/unclassified rather than being inferred from names. | `toMarketRow`; canonical market filters and low-tier tests; verified |
| Match phase/status | `phase` and market `status` enums | Phase appears as 直播/即将开始/已结束; closed/resolved rows remain identifiable as ended. Unknown phase does not invent a schedule. | `toMarketRow`; closed-market UI test; verified |
| Model coverage | `model_availability` is distinct from nullable `decision_action` | Available/unpromoted/not-yet-evaluated states use plain Chinese. Out-of-scope markets show no negative “not covered” warning; a null action is never rendered as a real decision. | `toMarketRow`; `p3-view-models.test.ts` low-tier and null-action tests; verified |
| Outcome/player names | Optional canonical `player_names` pair and IDs | Show actual player names only when matched; preserve real supplier market question otherwise. No vendor IDs are displayed. | `namesOf`, `toMarketRow`; `markets-page.test.tsx`; verified |
| Best ask by outcome | `quote.outcome_asks`, decimal strings in 0–1 range | Displayed as each player’s buy price in percent. Each side is independent; absent asks show “—”, never a complementary inferred price. | `toMarketRow`; per-outcome mapping test; verified |
| Best bid by outcome | `quote.outcome_bids`, decimal strings in 0–1 range | Used where an exit-side executable price is required; unavailable side stays absent. It is not substituted with ask or `1 - other outcome`. | Public DTO + decision workbench mapping; per-outcome-level and execution tests; verified |
| Quote state/source | `quote.state`, optional `quote.source` | User-facing states distinguish real-time update, latest snapshot, partial quote, no tradable quote, temporarily unavailable, previous valid quote, and limited data. Source enum stays internal. | `quoteStateLabel`; “labels every visible quote state” test; verified |
| Quote update time/freshness | `quote.as_of`; shown as elapsed time, not converted to a local wall clock | Quote age is taken from the quote itself, not market-catalog time. Stale text explicitly says “上次有效报价”; missing or malformed timestamp says “时间未知”. | `formatFreshness`, `quoteStateLabel`; quote freshness tests; verified |
| Spread | `quote.spread`, decimal fraction; arithmetic mean of the complete bid/ask spreads among available outcomes | Shown as “平均价差” percentage. The label tooltip says only outcomes with both sides contribute; null or malformed stays “—”. It is not the target player's executable entry/exit price. | `outcome_levels`; `toMarketRow`; market UI label/tooltip test; bug fixed and verified |
| Best-level notional | `quote.depth_usd`, USD decimal string; sum of price × size at each available outcome's best bid and best ask | Shown as “最优档金额”. Tooltip says this is only both outcomes' best levels, not the whole book or guaranteed fillable amount. Null or malformed stays “—”. | `outcome_levels`; `toMarketRow`; market UI label/tooltip test; bug fixed and verified |
| Model win probability | `MarketSummary.model_probability` is the predicted probability for outcome A / `player_names[0]`; `Opportunity.model_probability` is for `target_player_id`. Both are 0–1 or null. | The market card names the corresponding player in the probability label; opportunity cards pair the estimate with their explicit direction. Null means no estimate and never falls back to a market quote. | `P3QueryService.markets`; `toMarketRow`/`toOpportunityRow`; market-page mapping tests; ambiguous-label bug fixed |
| Decision reason | Optional canonical `reason_code` | Known reasons become concise Chinese explanations; unknown codes remain a neutral fallback and are not presented as a decision recommendation. | `REASON_LABELS`; reason-copy tests; verified |
| Opportunity eligibility/empty reason | Server `availability.reason` and `model_status` | Empty opportunity page distinguishes unpromoted model, no covered market, no eligible action, and data gap. Empty means no server-qualified row; the UI does not infer or fabricate a BUY/WAIT. | `OpportunityAvailabilityDto`; markets reason tests; verified |
| Opportunity target/action | `target_player_id`, `action`, paired player names | Target is selected only by matching canonical ID to the pair. The server action is translated to a Chinese state label; absent target is “—”. | `toOpportunityRow`; canonical API order/eligibility tests; verified |
| Model probability, executable price, edge | `model_probability`, fixed-$10 `executable_probability`, and decimal `conservative_net_edge` | Rendered respectively as model estimate, simulated average buy price, and percentage-point difference. Every value comes from server output; missing values stay “—”; maximum acceptable price is labeled as a reference, not a guaranteed fill. | `toOpportunityRow`; API view-model and opportunity UI tests; verified |
| Opportunity freshness/overlay | `as_of`, `is_stale`, `has_gap` | Stale/gap overlay preserves the last trusted row and explains that new simulated actions are paused. It does not relabel fresh market quote time as stale. | `toOpportunityRow`, `overlayOf`; stale decision tests; verified |
| Paper position identity/direction/status | Internal `position_id`/`match_id`, outcome player, canonical position status | Direction uses the identified player; status maps to readable simulated lifecycle states (pending, held, missed, exited, settled). Rows navigate only to an internal Match route. | `toPaperRow`, lifecycle label map; paper ledger tests; verified |
| Paper entry and shares | `entry_cost`, `shares`, `average_entry_price` | Filled positions show actual simulated spend, average fill price, and held shares. A pending-entry row comes from an intent: its stake/average are now labeled “计划投入 / 报价均价” and its known share quantity “预计份额”, not a completed buy. A missed entry has no position, so its zero ledger values render as “—” rather than implying money was spent or shares are held. | `_pending_entry_rows` vs `_position_dtos`; `PaperRow`, `PaperLifecycleLive`, preview ledger/lifecycle; pending and missed-entry regressions; bug fixed |
| Paper exit value and P&L | `current_exit_value`, `net_pnl` | `current_exit_value` is held shares × the current best bid for that outcome. It is a gross reference estimate: it does not walk book depth, deduct fees, or guarantee that the full position can be sold at that price. UI says “退出参考金额” and displays those limits. Net P&L is a separate ledger result; null stays “—”. | `P3QueryService._position_dtos` / `_paper_dtos`; `toPaperRow`; lifecycle and ledger copy tests; code-tested, wording bug fixed |
| Paper freshness | `freshness_as_of` from the position’s quote observation | Position freshness is not replaced by ledger/event update time; missing time is stated as unknown. | `toPaperRow`; backend query freshness tests and frontend mapping; verified |
| Decision/workbench gates and lifecycle | `DecisionSnapshotDto` action, gates, edge/prices, position, lifecycle, stale/gap and `as_of` | Match workbench shows model view, available market price, reason and simulated lifecycle. Technical enum/version/provider details stay out of ordinary user copy; stale/gap pauses new simulated actions. | `DecisionSummary` and lifecycle components; match decision/lifecycle tests; verified |
| Real-order boundary | P3 API exposes read-only market/decision data and paper ledger only | UI says simulated only; no wallet, real-order CTA, provider IDs, or submit action. Browser checks are read-only and paper controls use fixtures only. | `markets-page.test.tsx` no-wallet/no-trade test; Playwright P3 markets coverage; verified |

### Beijing-time consistency

- Backend relative-date and season boundaries use configured `TENNIX_PRODUCT_TIMEZONE` (default `Asia/Shanghai`). API-Tennis ranking snapshots convert the UTC observation instant to that timezone before deriving `ranking_date`.
- Frontend match schedules, match/profile snapshot times, result-row calendar dates/years, the initial profile season selector, and P3 market/workbench clocks use `Asia/Shanghai`; visible labels say “北京时间”. Player age also uses the Beijing calendar date.
- API-Tennis date-range requests still send `timezone=GMT`. Player-season requests query a small GMT date superset derived from the requested local boundaries, then filter timestamped finished matches by their canonical timestamp in the configured product timezone. This includes Beijing Jan 1 matches occurring before 08:00 local without admitting the previous local year; no extra provider request is made. Rows the supplier returns without a canonical start timestamp remain undated rather than being silently dropped, and make the season page partial because their exact local date is unknown.
- Earlier ROADMAP/spec/plan rows remain historical records of the previous Macau decision; `PROJECT.md`, `.env.example`, current product context, and this appendix supersede that historical wording.

## T98 Cross-Product API, Chat and Stream Field Matrix

Evidence in this appendix is limited to checked-in code, deterministic tests and demo/fixture data. It does not claim a live runtime or current vendor-data verification. Fields that exist only for internal correlation are explicitly identified rather than rendered as consumer copy.

### Home catalog and player resolution

| Contract fields | Source and consumer | Empty/invalid behavior | Evidence / status |
|---|---|---|---|
| `MatchCatalog.status`, `matches` | `/matches/catalog`; status selects Live vs Upcoming. Each canonical `MatchDto` flows through the existing T95 match matrix and Home match cards. Matches sort server-side by product priority; Home does not reorder provider fields. | A valid empty array is “暂无直播比赛” / “今晚暂无待开赛比赛”; a rejected request is a retryable friendly error, not an empty result. | `TennisService.list_catalog`; Home page and catalog tests; verified |
| `filters.circuits/genders/disciplines` | Echo of the active canonical filter sets; controls are rendered by the Home filter bar. Empty gender means all genders; circuits and disciplines use declared defaults. | Unknown filter enums fail request validation; no implicit fallback to an unrelated filter. | `MatchFilters`, `match_catalog`; filter tests; verified |
| `facet_counts.circuits/genders/disciplines` | Server counts for each facet while skipping that facet's own selected dimension; Home merges live/upcoming counts and uses them to show available choices and detect live matches outside current filters. | Counts are explicit zero when absent for a category; counts do not mean total catalog count. | `TennisService.list_catalog`; `mergeFacetCounts`/`hasLiveMatchesOutsideFilters`; service/filter tests; verified |
| `featured_match_id` | First product-sorted selected match ID; Home resolves it only against the returned live/upcoming page rows. | Null or an ID not in either response produces no featured card; no match is fabricated. | service catalog tests; Home featured tests; verified |
| Player-resolution `status/query/player/candidates` | REST `/players/search` and Chat `player_resolution`; status selects resolved, ambiguous or not-found presentation. `query` echoes the user's original text and is not shown as a separate field; the resolved `player` is conveyed by the answer, while `candidates` become selectable cards when ambiguous. | Not-found has no candidates; ambiguous lists selectable candidates; missing rank remains “暂无当前排名”. | `searchResolutionToEntries`; `HomeAssistant`; search and ambiguous-candidate tests; verified |
| Candidate `matched_alias/alias_kind/current_rank` | Alias/rank facts from local directory resolution. `current_rank` is displayed; matched alias and alias kind are intentionally not shown because they are search diagnostics, not player attributes. | Missing rank remains absent; no supplier abbreviation or alias is treated as the player's displayed name. | player resolution service tests; Home candidate test; code-tested |
| Player country `country_code/country_alpha2` | Canonical country code plus alpha-2 derived through the ISO registry; Home candidates and Players pages pass it through `countryPresentation` / `PlayerCountry`. | World/unknown country does not fabricate a national flag; known alpha-2 uses a generic flag resource rather than a hand-curated flag list. | T96 country tests and Home candidate flag regression; verified |

### Chat request, structured data and SSE

| Contract fields | Source and consumer | Empty/invalid behavior | Evidence / status |
|---|---|---|---|
| Chat request `scope/match_id/messages[].role/content` | Home submits `global`; Match submits `match` plus current internal match ID. `role` distinguishes user from assistant history; `content` is conversation text. Backend accepts 1–12 messages, only `user`/`assistant` roles, and 1–4000 characters per message. Provider IDs are not accepted from the model for match-decision context. | Invalid role, empty/oversized content, or message count fails request validation; missing Match context is typed unsupported; aborted requests are not committed as answers. | `ChatRequest`/`ChatMessage` Pydantic constraints; Chat API/tool scope tests; `useChatStream` tests; verified |
| `status.stage/phase/completed/total/tool` | Backend progress events; UI maps known stages to plain Chinese and displays bounded completed/total progress. `phase` and `tool` remain internal and are not shown raw. | Unknown stage uses neutral “正在查询…”; invalid or missing counters suppress progress fraction. | `chatStageLabel`, `chatProgressFromStatus`; hook tests; verified |
| `text_delta.delta`, `warning.code/message/details`, `error.code/message/details`, `done.ok` | Deltas form the visible answer; warnings show the supplied user-facing message only; error codes map to surface-specific copy and details are not dumped. `done.ok` gates completion. | Stream EOF before terminal event and `done(ok=false)` are errors; partial text may remain visible but is not added to conversation history. Unknown error codes use a safe fallback. | `use-chat-stream.ts`; terminal/EOF regression tests; bug fixed and verified |
| Structured `matches` / `match` | Match DTOs in each `data` event are aggregated and de-duplicated by internal match ID into Home result cards; simultaneous player history no longer suppresses them. | Missing scores/status details preserve explicit unknown labels from T95; empty match sets are not reported as empty when the result is a P3 type. | Home aggregation tests; bug fixed and verified |
| `intelligence.packet` | Backend builds the topic-scoped canonical packet for the LLM answer. The packet is also included in the Chat `data` event, but the frontend does not consume/render its fields; it displays the answer text and the Match page renders the authoritative snapshot. | Null/omitted quality stays absent; no inferred statistic/point/momentum. The packet is not a second user-visible source of truth. | `ChatOrchestrator`; `rg` consumer trace; T95 intelligence mapping and UI tests; verified |
| `player_history.player/scope/season/availability/season_record/empty_reason` and structured `matches` | History card renders player, scope, supplied match rows and season record. Availability controls empty copy and adds a short caveat for populated partial/stale results. `empty_reason` distinguishes no scoped results from unavailable season record and is also passed to the LLM; H2H auxiliary list counts in `metadata` are answer context, not raw JSON on screen. | Unavailable says “赛果暂不可用”; partial/stale empty results disclose possible incompleteness/age; only a complete empty result says “该范围暂无赛果信息”. Populated partial/stale results retain their supplied rows and disclose the limitation. | `historyEmptyCopy`/`historyQualityCopy`; `HomePlayerHistory`; empty/unavailable/partial/stale regressions; bug fixed and verified |
| `market_opportunities.opportunities/truncated` | Home now aggregates/deduplicates all opportunity data events into existing opportunity cards. `truncated=true` now visibly says only a partial list is shown; each row preserves server-supplied values and links to Match. | An empty list is a truthful empty result, never a fabricated action; truncated is still disclosed even if no rows are present. | Home P3 card and truncation regression; bug fixed and verified |
| `match_decision` | Emitted only for Match-scope `get_match_decision`; its ordinary Chat response remains natural-language text. Match's separate decision workbench reads its dedicated REST/SSE snapshot and renders all supported fields, so the Chat packet itself is not rendered as a duplicate card. | No P3 context yields typed unsupported; no decision snapshot is fabricated. | Chat tool scope tests; Match workbench DTO/view-model tests; code-tested |
| `unsupported.metadata.reason` | Broad-history unsupported has no reason; P3-disabled result carries `p3_disabled`. Home title now distinguishes the market-disabled state from unavailable historical results. | Unknown reason becomes generic unsupported title, not a false historical limitation. | Home tests for both paths; bug fixed and verified |
| `answer_context.match_id/state_version/as_of` | Internal match snapshot/version/time anchor; Match Chat compares version with current snapshot and warns when answer became outdated. | Missing context suppresses outdated warning; no time is inferred from browser clock. | Match sidebar tests and chat context tests; verified |
| Match stream `ready`, `match_delta`, `match_ended`, `heartbeat`, `error`; `id/state_version/as_of/changes/connection_status` | `ready.snapshot` is the initial state; accepted deltas replace/reconcile canonical snapshot and version; terminal event ends the connection; heartbeat only keeps the stream alive; event ID is a resume cursor, not a displayed tennis statistic. | Version gaps trigger safe rebaseline; malformed/unknown transport data does not become a score; unavailable/stale connection remains explicit. | `use-match-stream` contract tests and T95 stream parity; verified |

### Home market pulse and health

| Contract fields | Source and consumer | Empty/invalid behavior | Evidence / status |
|---|---|---|---|
| `PulseView.data` / `PulseRow.match_id/kind/action/phase/player_names/model_probability/executable_probability/conservative_net_edge/tournament_name/is_stale/has_gap/as_of` | `/markets/pulse`; each row becomes a Home “市场脉搏” card. Player names and tournament identify the match; `phase` and `action` become status badges; model probability, net edge and freshness are shown. For opportunity rows, `executable_probability` is the $10 simulated buy price; for open-position rows it is the current best-bid exit reference. Stale/gap flags add a warning overlay and stale rows say simulated actions are paused. `match_id` builds the internal Match link; `kind` affects row priority. | Null values stay “—”; absent player names use the existing neutral match-name fallback; stale/gap state is not presented as a fresh actionable quote. The position price is explicitly a reference and does not promise that the full holding can exit at that price. A BUY with null/closed phase is explicitly “状态未知” and sorted after known live/upcoming BUYs and WAIT, never silently promoted to upcoming. | `TennisService.pulse`; `toPulseRow`/`selectHomePulseRows`; live and preview `MarketPulse`; unknown-phase ordering regression; bug fixed and verified |
| `PulseRow.market_id`, `PulseView.has_open_position` | `market_id` is present for correlation but is not rendered on the Home card. The pulse ordering is already reflected by the returned rows; the card does not separately read `has_open_position`. | Internal IDs and redundant envelope metadata do not leak into consumer text. | DTO decoder, pulse service, and Home consumer inspection; verified |
| `GET /health` `status/service` | Basic API readiness response used by local checks; not fetched or displayed by consumer pages. | Failure is an availability problem, never converted into empty tennis or market data. | `/api/v1/health` route; launcher/readiness checks; internal-only |

### P3 market stream, errors and runtime health

| Contract fields | Source and consumer | Empty/invalid behavior | Evidence / status |
|---|---|---|---|
| Markets stream `ready` counts/availability | `/markets/stream`; initial aggregate snapshot provides counts and opportunity availability. UI uses REST page results for card content; stream ready is not rendered as a market row. | Zero opportunities can be explained by model/universe/gap status; it is not evidence of a UI-generated opportunity. | `markets_snapshot`; stream and empty-state tests; verified |
| `quotes_changed.sequence/count/as_of` | Catalog quote publisher; sequence deduplicates the notification and consumer schedules a bounded REST refresh; count/time are transport metadata, not shown as individual quote freshness. | Duplicate/old sequence ignored; exact quote time always comes from each quote's own `quote.as_of`. | `use-market-stream`; quote refresh tests; verified |
| `market_delta.market_id/sequence/book_hash/as_of`, `market_gap.market_id/reason/as_of` | Market book publisher; per-market cursor detects discontinuity, triggers refetch and leaves the last trusted view visible. IDs/hashes/reason are not user-facing copy. | Duplicate/old deltas ignored; gaps never synthesize a new price. | market stream tests; verified |
| `decision_delta.match_id/observation_version/action/as_of` | Decision publisher; per-match cursor triggers decision/workspace refetch. Action/time are validated as backend state, not inferred by browser. | Duplicate/old deltas ignored; a version gap triggers refetch and does not expose an untrusted action. | decision and market stream tests; verified |
| `paper_delta.state/id/reason/as_of` | Paper publisher after ledger state commit; causes paper/workspace refresh. State and reason are translated by paper lifecycle mappings; internal IDs are used only for lookup. | Unknown reason uses safe consumer fallback; no event is evidence of a real order. | Paper publisher/API/lifecycle tests; verified |
| `resolution_delta.market_id/status/rules_version/payouts/confirmed_at` | Only a provider REST `FINAL` resolution is routed through settlement and then published on `tnx:p3:resolution:{market_id}`. The event is emitted both after an immediate REST check triggered by a WS hint and by the scheduled fallback; repeated identical final payloads are deduplicated within the runtime. Frontend stores market ID/status/confirmation time, refreshes the REST-backed surfaces and freezes that market's later book deltas. | WS hints never settle Paper; unknown statuses fail decoding and missing payouts never become a settlement result. Event publication is a refresh/freeze signal, not a second settlement path. | `MarketHotPublisher.publish_resolution`; `LocalRuntimeDaemon._apply_final_resolution`; publisher/daemon/SSE tests; `useMarketStream` hook test; official Polymarket contract; code-tested |
| `ErrorResponse.error(code/message/details)/request_id` | Backend errors; Next proxy preserves the JSON envelope; client uses HTTP status/code/message/details to choose safe per-page copy. `request_id` is not shown or logged in consumer UI and remains support/diagnostic metadata. | Malformed error envelope uses generic safe fallback; provider URL/raw details are not rendered. | API client and surface error tests; code-tested |
| `/runtime/health` state, `sources`, counters, paper/model status, market coverage timestamps and counts | Internal local operations endpoint used by launcher/E2E readiness. No ordinary consumer page fetches or displays this payload. | `runtime_not_started`/missing data is an explicit typed state; health unknown is not converted into “healthy”. | runtime health schema/launcher tests; not-consumed by consumer UI |

### T98 visual baseline freshness

| Evidence | Current implementation | Conclusion / status |
|---|---|---|
| Home, Markets, Match and Players screenshots under `frontend/e2e/__screenshots__/{desktop,mobile}` | 76 screenshots were regenerated from the current UI using an isolated `/tmp` copy, fake provider/LLM, P3 disabled, and a fixed clock. Representative desktop/mobile pages were visually inspected. Playwright visual comparison: `30 passed / 4 skipped`. | Current demo-mode rendering matches the stored baselines. This is fixture evidence only, not live-provider or initialized-runtime evidence. |
| Visual test exclusions | Four P2 Replay visual checks skip under fake mode; P1 Match live/upcoming screenshot cases were excluded because the browser-launched backend's realtime path requires Redis. Ordinary deterministic API/Chat tests inject the existing in-memory `RealtimeBundle`, so their prior Redis connection failures are resolved without starting Redis. Pytest bootstraps `Settings(_env_file=None)` before importing the module-level app; the production model's root `.env` default is unchanged. | P1 Match screenshots and P2 Replay visuals remain unverified; latest repository-local backend suite passes `1332` tests and skips `128` opt-in/infrastructure cases, without reading root `.env`. |
| Screenshot regeneration boundary | Tests ran from `/tmp` with `env -i`; the isolated copy had no project credentials, used fake data, and did not read the repository `.env`, access the repository `.next`, initialize a database, or call real providers/LLM/Polymarket. Temporary test services stopped after each run. | Demo-only browser evidence; real runtime/provider validation remains pending the user's later initialization decision. |

### Exact DTO fields checked during T98

| Contract fields | Source and consumer | Empty/invalid behavior | Evidence / status |
|---|---|---|---|
| `GET /markets` envelope `data/page/page_size/total` | `data` is decoded into rows; `total` drives “已加载 X / Y 场” and the load-more condition. `page` and `page_size` are validated, while the client advances from its requested page and fixed local page size. | A zero total is a valid empty list; malformed counts fail decoding instead of silently truncating the catalog. | `decodeMarketPage`; `MarketsWorkspace.loadListings`; decoder and paging tests; verified |
| `GET /api/players/search` `status/query/player/candidates` and candidate `player/matched_alias/alias_kind/current_rank` | Both `getPlayers` and the production directory's `searchPlayerDirectory` unwrap and runtime-check the response before `searchResolutionToEntries` consumes it. Player identity/name and nullable alias/country/ranking fields are normalized/validated; candidate fields and the resolved/ambiguous/not-found shape are checked. | A malformed `resolved` response without a player, invalid candidate, or contradictory `not_found` data becomes a safe load error; only a valid `not_found` becomes an empty result. No raw response is shown. | `decodePlayerSearchResolution`; client tests for both helpers; Players directory error branch; bug fixed and verified |
| Chat `StructuredData.kind/matches/packet/resolution/player_history/market_opportunities/match_decision/metadata/answer_context` | `kind` selects the result path; each optional payload is consumed by its matching Home/Match surface or retained as backend answer context. `metadata` carries bounded auxiliary counts/reasons and is never dumped as raw JSON. | Absent variant payloads stay absent; unsupported results do not synthesize a match, history row, opportunity, or decision. | `StructuredToolResult`, `ChatEvent`, `useChatStream`, Home/Match consumers and Chat tests; verified |
| `IntelligencePacket.topic/match_id/state_version/as_of/status/players/tournament/round/scheduled_at/surface/indoor/format/winner/score/server/statistics/recent_points/momentum/key_points/quality` | Built from the canonical snapshot for the requested answer topic and supplied to the backend LLM. It is serialized in Chat data, but the frontend does not render or consume its fields; the natural-language answer is shown and Match cards use the authoritative snapshot. | Missing fields and unavailable quality stay null/empty/explicit; no missing statistic, point winner, momentum or match status is inferred. | `app/intelligence.py`; `_model_intelligence_packet`; Match snapshot matrix and Chat tests; verified |
| `MarketSummary.status/as_of/player_ids`; `Opportunity.tournament_tier`; `PaperPosition.market_id` | These are decoded server fields but are not currently rendered by the ordinary market/opportunity/paper cards. The card's visible match phase is `phase`; catalog observation `as_of` is not substituted for the quote timestamp; internal IDs remain lookup/correlation data. | `phase` comes from linked canonical match status only (`live`→live, `scheduled`→prematch, `finished`→closed); cancelled/postponed/unknown and unlinked matches remain null and render as “状态未知”. A market listing's `open`, `scheduled` or `unknown` order status does not establish match phase; only a `closed`/`resolved` market listing yields a closed market phase. | `decodeMarketSummary`/view-model trace; frontend phase tests; `test_only_match_lifecycle_statuses_determine_market_phase`; `test_unlinked_market_does_not_infer_match_phase_from_listing_status`; market projection suite `3 passed`; bug fixed and verified |
| `MarketQuote.source/best_bid/best_ask/outcome_bids` | The market list shows each player's `outcome_asks`; quote state and `quote.as_of` supply the label and freshness. These other quote fields are decoded but not shown on that card. Match decision workbench exit prices come from `DecisionSnapshot.outcome_levels`, not from this market-list DTO. | No missing bid is replaced by an ask or a complementary price. Missing quote fields remain null. | `decodeMarketQuote`; `toMarketRow`; workbench mapping; consumer trace; verified |
| `PaperPositionsView.open/recent` and position `position_id/match_id/market_id/outcome_player_id/status/entry_cost/shares/average_entry_price/current_exit_value/net_pnl/freshness_as_of` | `open` and `recent` remain separate server buckets; `open` can also include a pending intent row that has no position yet. Identity/direction/status and freshness map to the Paper view; `market_id` is not shown and `match_id` is only the internal destination. | Pending intent stake and quote values are shown as planned estimates; pending shares are estimates on the production ledger and remain unknown in the prototype when no estimate exists. Missed entries have no position, so spend, shares and P&L render as “—” even if the ledger stores zero. Nullable quote-derived amounts remain absent. | `decodePaperPositions`; `_pending_entry_rows`/`_position_dtos`; `toPaperRow`; `paper-row.test.tsx`, market ledger and lifecycle tests; bug fixed |
| `DecisionSnapshot` `match_id/market_id/action/reason_code/target_player_id/observation_version/model_probabilities/model_availability/quote_average_price/quote_side/conservative_net_edge/max_acceptable_price/hold_value/model_version/calibration_version/policy_version/data_version/gates/outcome_levels/position/lifecycle/is_stale/has_gap/lock_profit_available/as_of` | IDs and `observation_version` are internal lookup/cursor facts; action, reason, target-player probability, quote side/average, edge/limit, gates, outcome levels, position/lifecycle, freshness and lock-profit state feed the Match workbench. `hold_value` is decision-engine evidence, not presented as a market price. Version strings are decoded for audit but are not ordinary consumer copy. | A missing whole probability map stays null; a null value inside a present per-player probability map is malformed and now fails decoding instead of becoming `0`. Missing quote values stay “—”. | `decodeDecisionSnapshot`; `p3-workbench-models`; workbench UI/decoder tests; zero-coercion bug fixed and verified |
| Market stream `ready` `markets/opportunities/open_positions/availability`; decision stream `ready` `match_id/decision/observation_version/action` | Market ready counts are stored for stream state but not displayed in current consumer pages. Decision ready's nested `decision` initializes the Match workbench; its observation version sets the cursor. `match_id` and duplicate `action` are not separately displayed. SSE `id` is a resume cursor. | Empty counts are zero only when supplied; absent decision snapshot remains null and is not treated as an action. | stream decoders/hooks; ready/reconnect tests; verified |
| `resolution_delta.market_id/status/rules_version/payouts/confirmed_at` | Only a provider REST `FINAL` resolution is routed through settlement and then published on `tnx:p3:resolution:{market_id}`. The event is emitted both after an immediate REST check triggered by a WS hint and by the scheduled fallback; repeated identical final payloads are deduplicated within the runtime. Frontend stores market ID/status/confirmation time, refreshes the REST-backed surfaces and freezes that market's later book deltas. | WS hints never settle Paper; unknown statuses fail decoding and missing payouts never become a settlement result. Event publication is a refresh/freeze signal, not a second settlement path. | `MarketHotPublisher.publish_resolution`; `LocalRuntimeDaemon._apply_final_resolution`; publisher/daemon/SSE tests; `useMarketStream` hook test; official Polymarket contract; code-tested |
| `ResolutionPayoutDto.player_id/payout_per_share` | Backend serializes the canonical internal player IDs and payout decimals into the final event. The decoder validates both; the hook currently discards payout rows and does not render or settle from them. | Payout values are validated by the canonical backend model and are not used by UI or Paper client-side; persisted REST-confirmed resolution remains the sole settlement input. | `decodeMarketStreamEvent`; `OutcomePayout`; `MarketHotPublisher`; publisher and decoder tests; not-consumed by frontend |
| Runtime health `generated_at/sources/counters/paper_status/model_status/market_coverage`; source `status/reason_code/last_success_at/last_event_at/last_tracked/success_count/failure_count`; coverage `generated_at/candidate/attempted/fresh_realtime/fresh_snapshot/partial/no_liquidity/unavailable/stale/limited/batch_failures/rate_limited/retry_after_until/last_successful_batch_at` | Aggregate operational diagnostics consumed by local readiness checks, not by ordinary product pages. | Missing runtime record returns `runtime_not_started`; absent source/coverage details remain unknown rather than healthy or zero unless the DTO explicitly supplies a zero count. | Runtime DTO definitions, runtime health endpoint and launcher tests; internal-only |

### Confirmed fixes from T98 fixture audit

- `getPlayers()` previously typed `/api/players/search` as `PlayerDto[]` even though the endpoint returns a resolution envelope. It now returns `PlayerSearchResolutionDto`; client test uses the real envelope shape.
- Home Chat previously ignored earlier structured match events, treated any stream EOF as success/history, used a success icon even when no match card existed, and omitted structured market-opportunity cards. These paths now aggregate typed data, require a successful terminal event, and announce only cards actually present.
- Home ambiguous player cards previously rendered raw lower-case country codes instead of country/flag presentation; they now use the same canonical country presentation as other pages.
- Unknown market reason enums previously fell through to raw internal codes; they now use “暂无法提供判断原因”.
- Market cards previously called an average spread “买卖价差” and top-of-book notional “可交易金额”; labels/tooltips now describe the precise average and best-level scope.
- Home market pulse previously labeled an open position's best-bid exit reference as a $10 simulated buy price; position and opportunity rows now use distinct labels, with the position price explicitly qualified as a reference.
- Missing market/pulse phase values previously fell through to “已结束/已完赛”; they now retain an explicit unknown label, with view-model and rendered-card regressions.
- Market listing statuses (`open`/`scheduled`/`unknown`) previously fabricated a `prematch` phase when no linked match facts existed; linked cancelled/postponed/unknown match states also no longer masquerade as `closed`. Only canonical scheduled/live/finished match states, or a closed/resolved listing, determine the phase; projection regressions pass.
- A null value inside a present `model_probabilities` map previously became a fabricated 0%; the typed decoder now rejects the malformed map, while an entirely absent model map remains a valid unknown.
- Polymarket `market_resolved` subscription/event handling and immediate provider-finality recheck now follow the official hint→REST-authority boundary. No real WebSocket/provider request was made during this audit.
- Player-history cards previously rendered “该范围暂无赛果信息” even when the provider marked the result unavailable, and omitted partial/stale caveats when rows existed. Empty copy now separates unavailable, incomplete, stale and complete-empty states; existing rows remain visible with a short quality note.
- First Home catalog reads could hydrate a match before the one-shot demo player-directory seed finished, temporarily replacing known rankings with null; concurrent callers could also return while seeding was still in progress. Catalog reads and concurrent seeder calls now wait for the active seed, covered by regression tests. Latest focused backend suite: `103 passed`.
- Four deterministic API/Chat tests previously reached the default Redis client even though they exercise fake-provider HTTP contracts. Their fixtures now inject the established in-memory `RealtimeBundle`; the previously blocked four tests pass. Pytest bootstrap also prevents module-level app import from reading the host's root `.env` while leaving production Settings unchanged: a temporary non-sensitive sentinel `.env` first made the regression fail, then passed after the test-only isolation, and was removed. Repository-local `env -i` backend suite most recently passed `1332` tests and skipped `128` opt-in/infrastructure cases, with no Redis/PostgreSQL service and no root `.env` access.
