# TennixAI Agent 启动入口

本文件只定义协作流程，不保存项目状态。根目录三份总控是唯一权威入口。

## 开始任何工作之前

1. 按顺序完整阅读 `PROJECT.md`、`ROADMAP.md`、`CURRENT.md`。
2. 运行 `git fetch origin`、`git status --short --branch`、`git branch --show-current`、`git log -1 --oneline`。
3. P1 默认在 `main` 工作。工作区干净且本地落后时，用 fast-forward 同步 `origin/main`；本地超前、分叉、位于其他分支或有未说明改动时，先核对并处理，不能直接开发。
4. 将实际分支、HEAD 和工作区状态与 `CURRENT.md` 对照。代码、测试或 Git 与总控不一致时，以已验证事实修正总控后再继续。
5. 保留所有已有改动。遇到 `CURRENT.md` 未说明且无法判断归属的未提交改动时，停下询问用户，不覆盖、不删除、不顺手提交。
6. 同一时间只允许一个主任务处于 `in_progress`。开始任务前，在 `CURRENT.md` 写入执行者/ADE、`main`、任务起始提交和更新时间，提交并推送这次领取记录到 `origin/main`。
7. 如果任务已由另一执行者领取，不得静默并行；必须先完成显式交接。只有用户明确批准隔离实验或并行工作时才创建分支，并且必须先把分支信息写回并推送到 `origin/main`。

## 执行边界

- 只执行 `CURRENT.md` 的当前主任务；候选后续任务不构成授权。
- 详细实现按 `CURRENT.md` 链接的规格和计划执行，不在总控文件中复制整套步骤。
- 产品目标与优先级以 `PROJECT.md`、`ROADMAP.md` 为准；实现事实以代码、测试和 Git HEAD 为准。
- 不得把供应商、LLM、数据库、缓存或其他未来能力提前带入当前阶段。

## 更新与交接

- 产品边界或稳定架构改变：更新 `PROJECT.md`。
- 阶段、任务、顺序、验收门或完成证据改变：更新 `ROADMAP.md`。
- 任务开始、可验证节点完成、阻塞或交接：更新 `CURRENT.md`。
- `done` 必须附完成提交和实际通过的验证证据；没有运行的测试不能声称通过。
- 完成、阻塞或交接时，提交并推送任务内成果和相关总控更新。保留任务外改动。
- `CURRENT.md` 只保留最近交接和最多 5 条近期变更；长期记录进入 `ROADMAP.md`，完整审计由 Git 历史承担。
